import type { HttpResourceOptions } from '@angular/common/http';

/**
 * Options for creating an `OpenapiResource`.
 */
export type OpenapiResourceOptions<TResult, TRaw> = HttpResourceOptions<TResult, TRaw>;
